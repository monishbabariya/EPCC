[project]
name = "epcc-enums"
version = "0.5.0"
description = "EPCC ENUMs generated from X8_GlossaryENUMs (single source of truth). Do NOT hand-edit — run scripts/codegen-enums.py."
requires-python = ">=3.12,<3.13"
authors = [{ name = "Monish (generated)" }]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/epcc_enums"]
